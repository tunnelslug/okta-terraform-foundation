# STACK: policies
# Wire signon/mfa/password modules after modules/policies/* is present
# Placeholder – expand with module.signon, module.mfa, module.password

output "environment" {
  value = var.environment
}
